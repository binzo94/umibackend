import React from 'react'
import { withRouter } from 'react-router-dom'
import { Http } from '../../services'
import { Button, Table, Pagination } from 'element-react'
import './person-qualification-list.css'

class PersonQualificationForm extends React.Component {
  constructor(props) {
    super(props)
    this.state = {}
  }

  render () {
    return (
      <React.Fragment>
      </React.Fragment>
    )
  }
}

export default withRouter(PersonQualificationForm)
